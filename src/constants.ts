export enum ListenerNames {
  click = "onClick",
  input = "onInput",
  focus = "onFocus",
  blur = "onBlur",
}

export enum ScreensPathnames {
  login = "/",
  register = "/sign-up",
  settings = "/settings",
  messenger = "/messenger",
  screen500 = "/500",
}
